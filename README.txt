To launch the game, consider using a local development server to serve the project
This will prevent from CORS issues

Example using Node.js:

1. Open a terminal and navigate to the source of the project
2. Install http-server: "npm install -g http-server"
3. Run the local server: "http-server"
4. That's it !